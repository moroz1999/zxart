Нулевая задача: автонаполнение информации о железе в релизах.
1. Если у релиза есть формат - TZX, TAP (явно лента), TRD/SCL (явно TR-DOS, если это не CP/M), то мы можем при сохранении релиза автоматически заполнять железо прода. Если у релиза есть формат - DSK, то мы можем при сохранении релиза автоматически заполнять железо прода. Если у релиза есть формат - CPM, то мы можем при сохранении релиза автоматически заполнять железо прода. Если у релиза есть формат - CPM, то мы можем при сохранении релиза автоматически заполнять железо прода. Если у релиза есть формат - CPM, то мы можем при сохранении релиза автоматически заполнять железо релиза.
2. Проанализируй, какое есть железо в проекте. Какие бывают форматы и сочетания. Предложи набор правил, по которым можно это автозаполнить. 
3. Сделай одноразовый скрипт, который заполнит все релизы, которые уже есть в базе, по этим правилам. Пример в fix controller, чтоб я мог его запустить через web.

Первая задача: продвинуть управление списками поддерживаемого железа на новый уровень.
1. В базе это сейчас enum в таблице. Нужно перенести в отдельную таблицу, где будет id, code, name, category на трех языках. Подготовь миграцию в SQL, которую я сам на проде запущу.
2. Название железа сейчас лежит в фронтенде в переводах. Нужно составить план переделки фронтенда - полный поименный чеклист всех компонентов и их бэкендов. 
3. Новая привелегия на бэкенде на public root - editHardware. 
4. Новый раздел во фронте - admin. Там пока будет только подраздел hardware. Показывается ТОЛЬКО если у пользователя есть editHardware привелегия. Где должны приходить привилегии root? CurrentUser вызов?
5. В новом разделе будет список всего железа, поделенный на категории. Категории - это enum пока что. Возьми их из имеющихся.
6. +Форма редактирования, добавления и удаления железа. Логика и дизайн формы - ровно как у нас уже есть в других сущностях.
7. На бэке не используй систему экшнов легаси. Заведи отдельный контроллер и там проверяй привилегию, одну на все действия.

Вторая задача: возможность назначать железо на уровне прода. Результат, которого добиваемся: убрать дублирование информации о железе из релизов. Основной набор переедет в проды, а в релизах будут только специфические релизные адаптации. 
1. Сделай изменения в форме, в вьюхе, на бэке. Аналогично тому, что в релизе сделано. Выдели общий компонент, если его нет.
2. Выясни и составь полный список ВСЕХ фронтенд-компонентов, где используется железо - фильтры, карточки, таблицы итд. Составь список изменений, где надо будет учитывать железо не только релизов, но и продов.
3. Сделай одноразовый скрипт (пример в fix controller), чтобы я его через веб запустил на проде. Фикс должен будет собрать данные о железе из релизов и перенести их в проды. Логика такая: проходимся по всем релизам прода (кроме тех, у которых нет железа), собираем наименьший общий набор и пишем его в прод. А в релизе удаляем и оставляем только специфичные. Если у прода есть хотя бы один original release, ТО ЖЕЛЕЗО СОБИРАЕМ ТОЛЬКО С НЕГО.
4. Выясни, как это подействует на API фильтра и реализуй там тоже фильтрацию по железу прода, чтобы не отломать имеющееся.

