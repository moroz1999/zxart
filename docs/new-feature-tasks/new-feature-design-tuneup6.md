пока забей на MCP, не работает.
1. skeleton в prods grid у всех модулей на главной должен повторять отступы грида реального прода. там space between надо.
2. zx-input-range использует input range. zx-min-max-range использует mat-slider. давай задокументируем, что от material мы постепенно отказываемся и заменяем своими компонентами. Переделай оба range, чтобы внутри использовали кастомную реализацию, но чтоб их переменные (толщина линии, кружков, цвета, отображение выбранных значений) были единообразны. Цель - чтоб выглядели одинаково и работали идеально по UX.
3. Спиннер в выборе страницы выглядит хорошо, но должен быть слева, а не справа. 