import {ZxSelectOption} from '../zx-select/zx-select.component';

/** A titled group of options for the grouped variant of zx-multi-select-filter. */
export interface MultiSelectGroup {
  label: string;
  options: ZxSelectOption[];
}
