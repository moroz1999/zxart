/**
 * Minimal reference to a business entity (the value of an entity autocomplete /
 * relation picker): the structure-element id and its display title.
 */
export interface EntityRef {
  id: number;
  title: string;
}
